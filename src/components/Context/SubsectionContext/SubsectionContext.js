/**
 * @file Módulo responsável pela criação do contexto SubsectionContext para transmissão de informações 
 * quanto à subseção atual.
 * @copyright Lucas N. T. Sab 2023
 */
import { createContext } from 'react';

const SubsectionContext = createContext(null);

export default SubsectionContext;