/**
 * @file Módulo responsável por armazenar dragger utilizado entre quadros para focalizar elementos
 * em animação.
 * @copyright Lucas N. T. Sab 2023
 */
import { createContext } from 'react';

const DraggerContext = createContext(null);

export default DraggerContext;