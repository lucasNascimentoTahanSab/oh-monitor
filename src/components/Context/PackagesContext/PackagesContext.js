/**
 * @file Módulo responsável pela armazenamento dos editores de código e respectivos arquivos.
 * @copyright Lucas N. T. Sab 2023
 */
import { createContext } from 'react';

const PackagesContext = createContext(null);

export default PackagesContext;